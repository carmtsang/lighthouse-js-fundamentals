// ageCalculator function should take 3 parameters - name, yearOfBirth, currentYear

function ageCalculator (name, yearOfBirth, currentYear) {
  let age = currentYear - yearOfBirth;
  
  return name + " is " + age + " years old.";
}